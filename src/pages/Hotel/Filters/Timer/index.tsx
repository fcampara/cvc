import React from 'react'
import { ContainerTimer } from './styles'

const HolteFilterTime: React.FC = () => {
  return <ContainerTimer>Pesquisa válida por 00:00</ContainerTimer>
}

export default React.memo(HolteFilterTime)
